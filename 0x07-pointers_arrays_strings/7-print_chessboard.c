#include "main.h"

/**
 *print_chessboard- finds first occurrence of a first string
 *@a: pointer
 *
 *Return: return memory address
 */




void print_chessboard(char (*a)[8])
{

	int i, j;

	for (i = 0; i < 8; i++)
	{
		for (j = 0; j < 8; j++)
		{
			_putchar(a[j];
		}
		_putchar(a[i]);

	_putchar('\n');
	}

}
